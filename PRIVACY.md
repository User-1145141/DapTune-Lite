# 隐私说明

DapTune Lite 是一个本地优先的系统音效控制器。应用本身不提供联网功能，不声明
`INTERNET` 权限，不下载 AutoEq 或其他在线音频测量数据，也不连接 GitHub 检查应用更新。

DapTune Lite 不包含广告、分析、遥测、远程崩溃上报或将用户数据上传到开发者服务器的功能。

## 本地处理的数据

DapTune Lite 在应用私有存储中保存：

- 内置和用户创建的均衡器配置及 20 个 Q4 增益值；
- 自动切换、重启恢复和默认配置设置；
- 已发现播放设备的哈希路由键、显示名、类型和最后出现时间；
- 设备与配置的绑定以及最近应用状态；
- 自动切换和相关操作产生的本地日志，包括操作时间、动作、结果、路由显示名、
  配置名、验证方式和失败详情。

上述数据用于提供均衡器、配置管理、设备识别和自动切换功能，默认保存在设备本地。

### 设备标识

对于蓝牙与 LE Audio 设备，只有在完整地址能够由 Android 已配对设备清单唯一验证时，
DapTune Lite 才会创建稳定的本地设备键。该键在本机经过 SHA-256 截取后保存，原始
蓝牙地址不会写入应用数据库。

匿名蓝牙地址、设备名称或临时路由包装 ID 不会被作为持久设备身份。

USB、HDMI 等其他音频设备使用 Android 提供的本地路由身份生成哈希键。

设备显示名可能包含用户自行设置的名称。因此，在公开截图、日志或 Issue 中分享诊断信息
之前，应检查并删除其中可能存在的个人信息。

## 音频处理

DapTune Lite 不读取、录制、缓存或上传用户的音频样本。

应用只通过 Android 系统提供的音频效果接口控制设备上已经存在的全局厂商
`AudioEffect`，并根据系统提供的输出路由信息识别当前音频输出设备。

均衡器参数由应用在本地处理，不会因为使用均衡器而上传音频内容。

## 权限

DapTune Lite 使用的系统权限及其用途如下：

| 权限 | 用途 |
|---|---|
| `MODIFY_AUDIO_SETTINGS` | 访问并控制系统现有的音频效果 |
| `BLUETOOTH_CONNECT` | 在支持的 Android 版本识别当前蓝牙音频输出 |
| `POST_NOTIFICATIONS` | 显示自动切换前台服务所需的通知 |
| `FOREGROUND_SERVICE_SPECIAL_USE` | 在用户开启自动切换后保持设备变化监听 |
| `RECEIVE_BOOT_COMPLETED` | 在设备重启后恢复用户之前开启的自动切换功能 |

DapTune Lite 不声明麦克风、摄像头、位置、联系人或媒体读取权限。

拒绝通知权限不会授予 DapTune Lite 任何额外的数据访问权限。Android 13 及更高版本中，
系统可能允许前台服务继续运行，但其通知是否显示在通知抽屉中取决于系统行为及厂商实现。

## 网络访问

DapTune Lite 不提供 AutoEq 下载、在线曲线检索或应用在线更新功能。

应用本身不需要 `INTERNET` 权限，也不会将均衡器配置、设备信息、日志或音频内容发送到
开发者服务器。

如果 Android 系统、设备厂商或用户安装的其他软件对应用进行网络层面的处理，该行为不属于
DapTune Lite 自身提供的数据上传功能。

## Android 系统备份

应用可以使用 Android Auto Backup 和设备迁移机制。数据库与 DataStore 中的数据可能由
Android 操作系统根据用户的系统备份设置复制到用户选择的备份提供程序。

DapTune Lite 自身没有独立的数据上传服务，也无法控制 Android 备份提供程序对备份数据的
保存、同步、保留或删除周期。

如果不希望系统备份 DapTune Lite 的数据，请在 Android 系统的备份设置中关闭相应备份功能。

## 查看与删除

- 配置页可以删除用户创建的配置；
- 自动切换页面可以删除历史设备；
- 日志页面可以清空操作日志；
- Android 系统的“清除存储”会删除应用在设备上的本地数据；
- 卸载应用通常会删除其应用私有数据，但 Android 系统备份提供程序可能按照自身策略继续
  保留已有备份。

## Root / Magisk / KernelSU 模块

实验性的 Magisk / KernelSU 工具属于主应用之外的独立系统模块。

卸载 DapTune Lite 不会自动删除已经安装的 Magisk / KernelSU 模块或其本地状态文件。
如果不再使用这些模块，应通过对应的 root 管理器单独卸载。

## 分享诊断信息

DapTune Lite 的日志或系统转储可能包含设备名称、应用包名、时间、UID、蓝牙相关信息、
设备序列号或其他系统环境信息。

在公开 GitHub Issue、论坛或其他平台分享日志和诊断信息时，请只提交复现问题所需的最小
片段，并提前删除个人标识、网络地址、账户信息、媒体名称以及其他可能暴露个人信息的内容。
