# 隐私说明

DapTune 是本地优先的音效控制器。应用清单声明 `INTERNET`，用于 AutoEq 官方索引与所选
`GraphicEQ` 下载，以及从本项目 GitHub Release 元数据检查正式版本。应用不声明麦克风、摄像头、位置、联系人或
媒体读取权限，没有分析、广告、遥测、远程崩溃上报或用户数据上传端点。

AutoEq 耳机型号索引下载后在本地检索；搜索词不会作为查询参数发送。官方索引缓存最多七天，并可能包含
上游收录的耳机型号、测量来源和形态。清除应用缓存会删除它。已导入的曲线作为普通本地配置保存。

## 本地处理的数据

DapTune 在应用私有存储中保存：

- 内置和用户创建的均衡器配置及 20 个 Q4 增益值；
- 自动切换、重启恢复和默认配置设置；
- 已发现播放设备的哈希路由键、显示名、类型和最后出现时间；
- 设备与配置的绑定、最近应用状态；
- 操作时间、动作、结果、路由显示名、配置名、验证方式和失败详情。

蓝牙与 LE Audio 只有在完整地址能由 Android 已配对设备清单唯一验证时才创建稳定键，之后在本机经过
SHA-256 截取；原始地址不写入数据库。匿名地址、设备名或路由包装 ID 不会作为持久设备身份。USB、HDMI
等设备继续由本机路由身份生成哈希键。设备显示名可能包含用户自定义名称，公开截图或日志前仍应检查并脱敏。

## 音频与权限

DapTune 不读取、录制、缓存或上传音频样本。它只向 Android 已存在的全局厂商 `AudioEffect` 写 20 个
增益参数，并监听系统报告的输出路由元数据。

权限用途：

| 权限 | 用途 |
|---|---|
| `INTERNET` | 下载 AutoEq 官方内容，并读取本项目 GitHub Release 元数据检查更新 |
| `MODIFY_AUDIO_SETTINGS` | 获得并控制系统现有 DAP effect |
| `BLUETOOTH_CONNECT` | 在支持的 Android 版本识别当前蓝牙音频输出 |
| `POST_NOTIFICATIONS` | 显示低优先级自动切换前台服务通知 |
| `FOREGROUND_SERVICE_SPECIAL_USE` | 在设备变化时保持用户开启的自动切换监听 |
| `RECEIVE_BOOT_COMPLETED` | 用户允许时，在设备重启后恢复已开启的自动切换 |

拒绝通知权限不会授权任何额外数据处理。Android 13+ 的 AOSP 行为是前台服务仍可运行，但通知不显示在
通知抽屉；厂商系统可能有额外后台策略。

## Android 系统备份

应用启用 Android Auto Backup 和设备迁移，数据库与 DataStore 可由操作系统按用户的系统备份设置复制到
用户选择的备份提供程序。DapTune 自身没有上传代码，也无法控制该提供程序的保留和删除周期。若不希望
系统备份，请在 Android 的备份设置中关闭该应用或系统备份。

## 查看与删除

- 配置页可删除用户配置；
- 自动页可删除历史设备，日志页可清空操作日志；
- Android“清除存储”会删除本机所有 DapTune 数据；
- 卸载会删除本机应用私有数据，但系统备份提供程序可能按其策略保留备份。

实验性的 Magisk/KernelSU 工具是主应用之外的独立系统模块；卸载 DapTune 不会删除该模块或其本地状态
文件，应在 root 管理器中单独卸载。

## 分享诊断信息

日志和系统转储可能包含设备名、应用包名、时间、UID、蓝牙地址、设备序列号或其他环境信息。公开 Issue
只提交复现所需的最小片段，并先删除个人标识、网络地址、账户信息、媒体名称和厂商专有文件。
